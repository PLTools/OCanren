(library
  (name ppx_noinjected)
  (public_name OCanren-ppx.ppx_noinjected)
  (kind ppx_rewriter)
  (libraries base ppxlib)
  (modules ppx_noinjected)
  (flags (:standard -warn-error -A -rectypes))
  (preprocess (pps ppxlib.metaquot))
)


