(include_subdirs unqualified)

(library
  (name ocanren)
  (libraries GT GT.syntax.all)
  (flags (:standard -warn-error -A -rectypes))
  (preprocess
    (action (run camlp5 %{read-lines:../config/camlp5-flags} %{read-lines:../config/gt-flags} %{input-file}))
  )
)

